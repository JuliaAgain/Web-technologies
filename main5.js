const paginationButtons = document.querySelectorAll('.pagination');
for (let i = 0; i < paginationButtons; i++) {
    const Buttons = paginationButtons[i];
    Buttons.addEventListener('click', function(){
        const page = button.getAttribute()
    })
}